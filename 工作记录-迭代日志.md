# 设计工作台（Design Workbench）工作记录 · 迭代日志

> 项目路径：`/Users/zhuoying/WorkBuddy/2026-08-14-10-10-04/design-workbench`
> 技术栈：React + Vite + TypeScript，原生 Canvas 图像处理，纯前端、本地处理、文件不上传
> 线上地址：**https://design-workbench.netlify.app/**
> 代码仓库：**https://github.com/zhuohh599-maker/design-workbench**

---

## 一、项目概况

浏览器版设计小工具集合工作台，可作为后续封装桌面 App（Tauri）的基础。核心特点：

- **纯前端、零后端**：所有图像/视频处理在浏览器本地完成，素材不上传服务器。
- **工具注册表架构**：`src/core/registry.tsx` 定义 `ToolDef` 接口 + `React.lazy` 代码分割。新增工具只需写一个组件、在注册表追加一条，自动获得侧边栏入口与懒加载。
- **部署**：通过 GitHub + Netlify 自动部署，改完代码推一次即自动重新发布。

### 已上线工具（7 个）

| 分组 | 工具 | 说明 |
|------|------|------|
| 图片处理 | 尺寸缩放 | 缩放/裁剪到指定尺寸，锁比例 + cover/contain/stretch 三种适配 |
| 图片处理 | 格式转换 | 图片格式互转（PNG/JPG/WebP 等） |
| 图片处理 | 智能压缩 | 导出前自动压缩体积（GIF 重新量化 / PNG 转 WebP / JPG 重编码） |
| 图片处理 | AI 抠图 | 本地 ONNX 模型去背景（`@imgly/background-removal`） |
| 图片处理 | 图片圆角 | 四角圆角裁切，默认四角一致、可独立设置，导出透明 PNG |
| 动图制作 | 动图制作 | 画板 + 多图层 + 单元素动效（脉冲/滑动/扫光/淡入/旋转），导出 GIF/APNG |
| 视频处理 | 绿幕视频抠像 | 绿幕 MP4 逐帧抠像 + 水印抹除 + 自定义尺寸合成，导出透明 GIF |

---

## 二、迭代日志

### 1. 基础搭建（初始）
- 确定形态：浏览器版先行 + 预留 Tauri 封装口子；视频类功能先砍，后续加回。
- 技术选定：React+Vite+TS；抠图用 `@imgly/background-removal`（本地 ONNX）；GIF 用 `gifenc` + `gifuct-js` 解码。
- 落地工具注册表架构，实现 5 个基础工具（尺寸缩放 / 格式转换 / 智能压缩 / AI 抠图 / 动图制作）。

### 2. 动图制作增强（10:55–11:04）
- 画布默认取上传素材最大自然尺寸；新元素默认 100% 居中。
- 画布内变换控件：拖本体移动、拖角缩放、拖柄旋转。
- 图层列表支持排序（↑↓ 调层级）与显隐（👁/🚫），隐藏层绘制与导出均跳过。
- 动效模型重构为 `effects` 数组，支持同元素叠加多个动效；新增淡入淡出、旋转两类动效。
- 扫光改为 `source-atop` 隔离 + 光带宽度/强度/角度可调；图层显示缩略图（透明棋盘格底）；顶层在列表最上方。

### 3. 绿幕视频抠像（11:42，新功能）
- 加回视频类：绿幕 MP4 → 逐帧抠像 → 水印框选抹除 → 自定义尺寸画布摆位 → 导出透明 GIF。
- 路线：不引入 ffmpeg.wasm，用 `<video>` + Canvas 逐帧解码/抠像（轻量、纯前端）。
- 算法：到目标绿的颜色距离控 alpha + 边缘柔化 + 绿溢色抑制；水印区域强制透明。
- **Bug 修复（11:49）**：初版上传无反应——JSX 漏渲染 `<video>` 元素，补上离屏隐藏的 video 后正常。

### 4. GIF 导出质量优化（11:55）
- 问题：导出 GIF 有颜色过渡锯齿/色带，预览正常。
- 根因：GIF 仅 256 色，量化无抖动导致渐变被分块。
- 修复：引入 `upng-js` 新增 APNG 真彩色导出；`encodeGif` 加 Floyd-Steinberg 误差扩散抖动。

### 5. GIF 抖动修正 + 拖拽修复（13:11）
- 抖动无效根因：量化前做 RGB 误差扩散被 PnnQuant 重新聚类抵消。改**基于调色板的 Floyd-Steinberg**（量化后逐像素找最近色+误差扩散），对 banding 有效。
- 限制告知：GIF 透明是 1-bit alpha，半透明边缘必硬切，要一致须用 APNG。
- 修复绿幕视频拖拽上传无反应（补 `onDragOver/onDrop`）。

### 6. 全工具拖拽 + 导出智能压缩（13:18）
- 拖拽逻辑下沉到通用 `DropZone`，所有图片工具自动获得拖拽；动图画布支持拖图入画板。
- 新增 `core/utils/compress.ts` 智能压缩：按 MIME 分发（GIF 重量化 / PNG→WebP / JPG 重编码 / APNG 逐帧重编码），五个导出区接入，下载前压缩并提示体积变化。

### 7. GIF 主推 + 杂色扩散（13:28）
- 用户决策 GIF 使用更多 → GIF 设主按钮（推荐），APNG 降为备选。
- 抖动算法换成**杂色扩散（noiseDiffuse）**：基于坐标的稳定伪随机噪声，打散 banding 且不随帧变化（避免动画"沸腾"）；半透明边缘改成随机散布不透明小点，实现任意背景下的羽化。

### 8. 粘贴导入（13:32）
- 新增 `usePasteImport` 钩子：监听 `Ctrl/⌘+V`，从剪贴板取图片喂给工具。四个图片工具 + 动图画板自动获得粘贴导入。

### 9. 布局：预览固定 + 右侧独立滚动（13:45）
- 左侧预览区始终可见，右侧调整区独立滚动（`.tool-body` 双列 grid，`.panel` 独立 `overflow-y`）。一次 CSS 改动全局生效。

### 10. 精简导出区文案（13:51）
- 清理动图/绿幕导出区冗余按钮文案与提示，只保留「导出 GIF / 导出 APNG」+ 一句话说明。

### 11. 个人素材库（14:17，后已移除）
- 新增「上传自动入库 + 可收起抽屉」个人素材库（IndexedDB 存原图+缩略图，点缩略图插回工具，支持删除/清空/清理建议）。
- **Bug（14:34）**：入库后列表不刷新——每次操作 `db.close()` 关掉缓存的同一连接，导致后续读取抛错被吞。改为单页内长连接。
- **整体移除（14:36）**：用户要求去掉该功能，删除 `library.ts`/`libraryContext.tsx` 及所有接入代码、样式。
- **连带修复**：移除时误删 `ingest` 函数引用，导致上传无反应——改回直接 `onFile(f)`（14:47 前后修复）。

### 12. 左侧边栏可收起 + SVG 图标（15:06）
- 侧边栏可收起为仅图标态（64px），品牌/分组标题/工具名收起时隐藏。
- 新增 `icons.tsx`：7 个自定义 SVG 线形图标（未选中线形描边、选中面性蓝底白图标）。`ToolDef.icon` 类型由 string 改为组件。

### 13. 图片圆角工具（15:26，新功能）
- 上传图片 → 四角圆角裁切（默认四角一致，可取消关联独立设置每个角）→ 导出透明 PNG。
- 注册到「图片处理」分组，懒加载；新增 `RoundedIcon` 与圆角网格样式。

### 14. 部署上线（15:42–16:26）
- **部署准备**：`git init` + commit（main 分支，27 文件）；新增 `netlify.toml`（`npm run build` → `dist`）；`.gitignore` 补 `.workbuddy`。
- **GitHub 推送**：用户 `zhuohh599-maker` 在 GitHub 建空仓库 `design-workbench`，本地 `git remote add origin` + GitHub Desktop 推送成功。
- **Netlify 自动部署**：连 GitHub 授权 → 选仓库 → 因 `netlify.toml` 零配置 Deploy → 自定义子域 `design-workbench.netlify.app` → 设为 Public。
- **验证**：线上页面正常加载，全部工具可用。

---

## 三、关键技术决策与坑

1. **GIF vs APNG**：GIF 仅 256 色 + 1-bit 透明，半透明边缘必硬切；要"与预览一致"用 APNG 真彩色。实际导出以 GIF 为主（用户场景更多），杂色扩散缓解 banding。
2. **绿幕不引 ffmpeg.wasm**：`<video>` + Canvas 逐帧处理足够轻量，避免大体积 wasm 依赖。
3. **素材库已弃用**：IndexedDB 连接管理坑已踩过，最终按用户要求整体移除；如需恢复有完整代码记录。
4. **历史 typecheck 报错**：`gif.ts`/`compress.ts` 在 TS5.5 下有 Uint8Array 泛型 + 缺 `@types/gifenc` 报错，非阻塞（实际构建走 esbuild 通过）。

---

## 四、后续可选项（未做）

- **Tauri 封装**：打包成 macOS .app（已预留架构口子）。
- **圆角工具增强**：填充背景色导出 JPG、按短边百分比设圆角。
- **素材库重做**：若要恢复，建议按"发布成网页"场景用 IndexedDB 长连接方案。
- **绿幕/动图未做**：MP4 压缩转码（非绿幕类视频处理）尚未接回。
- **自定义域名**：Netlify 支持绑定自有域名（顶级域可能收费）。

---

_记录日期：2026-08-14_
